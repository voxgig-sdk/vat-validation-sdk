# Entity: geolocate

main: kit: entity: geolocate: {

  alias: field: {}

  name: "geolocate"
  op: {
    load: {
      name: "load"
      points: [
        {
          method: "GET"
          orig: "/geolocate"
          segments: [
            {
              lit: "geolocate"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.ip`"
          }
        }
      ]
    }
  }



}
