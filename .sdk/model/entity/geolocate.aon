# Entity: geolocate

main: kit: entity: geolocate: {

  alias: field: {}

  name: "geolocate"
  op: {
    load: {
      name: "load"
      points: [
        {
          method: "GET"
          orig: "/geolocate"
          parts: [
            "geolocate"
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.ip`"
          }
        }
      ]
    }
  }



}
